/**
 * POS Presentation - Composables
 * 
 * Responsabilidad: Lógica de UI reutilizable para el módulo POS.
 * 
 * IMPORTANTE:
 * - Los composables SOLO contienen lógica de UI
 * - Manejo de carrito de venta (UI state)
 * - Calculadora de totales (UI)
 * - Selector de productos
 * - NO ejecutan CRUD
 * - NO llaman APIs directamente
 * - NO contienen lógica de aplicación
 * 
 * CRUD siempre pertenece a la capa Application (store).
 * 
 * Ejemplo:
 * export { useCartUI } from './use-cart-ui.js';
 * export { useProductSearch } from './use-product-search.js';
 */

export {};

/**
 * IAM Presentation - Composables
 * 
 * Responsabilidad: Lógica de UI reutilizable para el módulo IAM.
 * 
 * IMPORTANTE:
 * - Los composables SOLO contienen lógica de UI
 * - Manejo de formularios de login/registro
 * - Validación de UI (no de dominio)
 * - NO ejecutan CRUD
 * - NO llaman APIs directamente
 * - NO contienen lógica de aplicación
 * 
 * CRUD siempre pertenece a la capa Application (store).
 * 
 * Ejemplo:
 * export { useLoginForm } from './use-login-form.js';
 * export { usePasswordValidation } from './use-password-validation.js';
 */

export {};

/**
 * Kitchen Domain - Value Objects
 * 
 * Los Value Objects son objetos inmutables que se comparan por valor.
 * Contienen validación y reglas propias del concepto de dominio que representan.
 * 
 * Ejemplo:
 * export { OrderNumber } from './order-number.value-object.js';
 * export { PreparationTime } from './preparation-time.value-object.js';
 */

export {};

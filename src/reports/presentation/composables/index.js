/**
 * Reports Presentation - Composables
 * 
 * Responsabilidad: Lógica de UI reutilizable para el módulo Reports.
 * 
 * IMPORTANTE:
 * - Los composables SOLO contienen lógica de UI
 * - Manejo de filtros de fecha/rango
 * - Selector de tipo de reporte
 * - Formato de visualización
 * - NO ejecutan CRUD
 * - NO llaman APIs directamente
 * - NO contienen lógica de aplicación
 * 
 * CRUD siempre pertenece a la capa Application (store).
 * 
 * Ejemplo:
 * export { useDateRangePicker } from './use-date-range-picker.js';
 * export { useReportFilters } from './use-report-filters.js';
 */

export {};

/**
 * Tables Presentation - Composables
 * 
 * Responsabilidad: Lógica de UI reutilizable para el módulo Tables.
 * 
 * IMPORTANTE:
 * - Los composables SOLO contienen lógica de UI
 * - Manejo de layout de mesas (visual)
 * - Drag & drop de asignación (UI)
 * - Estados visuales de mesas
 * - NO ejecutan CRUD
 * - NO llaman APIs directamente
 * - NO contienen lógica de aplicación
 * 
 * CRUD siempre pertenece a la capa Application (store).
 * 
 * Ejemplo:
 * export { useTableLayout } from './use-table-layout.js';
 * export { useTableDragDrop } from './use-table-drag-drop.js';
 */

export {};

/**
 * Inventory Domain - Value Objects
 * 
 * Los Value Objects son objetos inmutables que se comparan por valor.
 * Contienen validación y reglas propias del concepto de dominio que representan.
 * 
 * Ejemplo:
 * export { ProductSku } from './product-sku.value-object.js';
 * export { StockQuantity } from './stock-quantity.value-object.js';
 */

export {};

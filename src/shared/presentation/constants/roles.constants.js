/**
 * Shared Presentation — Role & Permission Constants (re-export domain).
 */

export {
    ROLES,
    ROLE_ALLOWED_ROUTES,
    BRANCH_REQUIRED_ROUTES,
    hasRouteAccess,
    requiresBranch,
} from '../../domain/roles.js';
